# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What LiftOn is

LiftOn is a homology-based genome-annotation lift-over CLI. Given a reference genome `R`, its annotation `R_A`, and a target genome `T`, it produces an annotation `T_A` for `T`. It combines DNA-level alignment (a vendored fork of Liftoff, which can shell out to `minimap2` *or* drive it in-process via the `mappy` PyO3 binding) with protein-level alignment (`miniprot` invoked as a subprocess, with a `pyminiprot`-shaped facade ready to drop in a real PyO3 binding when one ships) and merges the two via an in-house "protein-maximization" chaining algorithm + ORF-rescue pass. CLI entry: `lifton.lifton:main` (`setup.py:34` → `lifton/lifton.py:649`, which calls `run_all_lifton_steps` at `lifton/lifton.py:283`). A second console script `gff3-validate = lifton.gff3_validator:_main` ships from the same package.

## Environment & commands

The project requires native deps (`parasail`, `pysam`, `pyfaidx`, `gffutils`, `duckdb`, `pyarrow`) that ship as bioconda/conda-forge wheels. On macOS/ARM, `pip install parasail` will fail to build from source — use conda. The vendored `lifton/gffbase/` ships a pre-built Rust extension (`_native*.so`); a missing extension falls back to the pure-Python parser at `lifton/gffbase/_pyfallback/`.

```bash
# one-time env (mirrors lifton.yml — Python 3.11)
conda create -n lifton-test -y python=3.11
conda activate lifton-test
conda install -y -c bioconda -c conda-forge \
    parasail-python pysam pyfaidx gffutils intervaltree \
    biopython networkx ujson cigar pytest coverage duckdb pyarrow
pip install mappy   # Phase 16 Tier 5: real --native minimap2 path; also unblocks test_native_bindings.py
pip install -e .

# Run the test suite (524 tests collect; fully hermetic re: minimap2/miniprot —
# they're monkey-patched to raise). 3 files (test_property_based, test_streaming_property,
# test_vulnerabilities) ERROR on collection unless `hypothesis` is installed; without `mappy`
# in the env, ~5 test_native_bindings cases fail (they assert mappy is present).
pytest tests/ -v

# Single test / single class / single case
pytest tests/test_lifton_class.py -v
pytest tests/test_lifton_class.py::TestUpdateCdsListSingleCds -v
pytest tests/test_lifton_class.py::TestWriteEntry::test_tmp_gene_skips_gene_line -v

# The 24-cell byte-identity gate (the load-bearing contract for refactors)
pytest tests/test_native_matrix.py::TestFullNativeMatrix::test_all_24_combinations_byte_identical -v

# Coverage (excludes vendored Liftoff)
coverage run --source=lifton --omit="lifton/liftoff/*" -m pytest tests/ -q
coverage report --skip-covered

# Run LiftOn against real data (requires minimap2 + miniprot on PATH)
lifton -g <ref.gff3> <ref.fa> <target.fa> -o lifton.gff3
# Skip the external runners by reusing prior outputs:
lifton -g <ref.gff3> -L <prior_liftoff.gff3> -M <prior_miniprot.gff3> <ref.fa> <target.fa> -o out.gff3
# Built-in timing harness:
lifton ... --measure_time   # writes time.txt
# Validate a standalone GFF3 file:
gff3-validate path/to/out.gff3
```

CI (`.github/workflows/tests.yml`, **Python 3.11** via `actions/checkout@v6` + `actions/setup-python@v6` — the Node24-native action majors, as of Iteration 14) installs `minimap2` + `miniprot` (+ `hypothesis`) and runs `flake8` (E9/F63/F7/F82 hard-fail, then a soft-warn lint pass), then **`pytest tests/`** — the **FULL 662-test suite** (Iteration 14 — the load-bearing 24-cell byte-identity matrix + the parallelism/promotion gates + the unit/integration suite + the 3 `hypothesis`-backed property/vulnerability files; `hypothesis` is `pip install`ed in CI but kept OUT of `install_requires`, so no `--ignore` is needed and local runs lacking it still skip those 3), then the `test/lifton_chr22_example.sh` shell example. So the byte-identity contract is now **CI-enforced** on every push/PR (previously CI ran only flake8 + the shell example — the test suite was unprotected, *and* CI had been silently RED for ~6 iterations on a pre-existing flake8 `F821` in vendored `gffbase/gffwriter.py`, masking everything downstream). **Build/robustness fixes that got CI green:** (1) `cigar` 0.1.3's sdist imports `pkg_resources` (removed in setuptools 81+), so CI pins `setuptools<81` + pre-builds cigar `--no-build-isolation`, then a normal `pip install -e .` (mappy keeps its own isolated Cython build) + `zlib1g-dev` for mappy's sdist; (2) the chr22 example's `test/chm13_chr22.fa.mmi` is **git-LFS-tracked**, and `actions/checkout` (no `lfs: true`) checks it out as a ~134-byte LFS *pointer*, not the 151 MB index — minimap2 reads the pointer → 0 alignments → empty `liftoff.gff3` → exit 1; the step `rm -f`s it so Liftoff rebuilds the index from the regular (non-LFS) `chm13_chr22.fa` (NOT a code/dep/lift-gene-like regression — the example passes locally with the identical minimap2 2.28 + deps). A `concurrency` block (`cancel-in-progress`) drops superseded runs, `timeout-minutes: 30` caps hangs, `cache: pip` (keyed on `setup.py`) avoids rebuilding mappy from sdist each run, and the two release-tarball `wget`s retry. The **Python-version skew** is closed: CI 3.11 matches `lifton.yml` (3.11), and `setup.py` `python_requires` was bumped `>=3.6` → `>=3.9` (a realistic floor — the code uses PEP-585 builtin generics, native in 3.9). pytest remains the right local gate to run before any change lands.

### Optional fast-path flags

Several flags toggle alternate paths. The four I/O / scheduling fast-paths (`--stream`, `--inmemory-liftoff`, `--threads`/`--locus-pipeline`, `--native`) are **byte-identical to the default output** — the 24-cell matrix test pins this; use them to optimise wall-clock or memory, never to change algorithms. A fifth scheduling optimisation, **concurrent Step 4** (overlap Liftoff‖miniprot), was **PROMOTED to the default** (Iteration 6) as a byte-neutral flip — **`--serial-aligners`** restores the old sequential path and **`--parallel-aligners`** is a kept no-op alias; its own `tests/test_parallel_aligners.py` pins the byte-identity. Two (`--strict-gff`, `--validate-output`) are validation gates that don't alter output bytes but can change the exit code. Three **change output**: **`--legacy-merge`** restores the pre-promotion *unconditional* Liftoff↔miniprot merge (the default now runs the verified **best-of-outcome** merge); **`--full-dp-align`** restores the pre-Iteration-3 *exact giant-only* alignment (the default now "bands everything" — anchor-windowed above ~2500 aa / 8000 nt; see *The byte-identity contract*); and **`--gene-only`** restores the pre-Iteration-12 *gene-only* lift (the default now lifts every auto-detected gene-like parent type — pseudogenes, ncRNA_genes, … — beyond just `gene`, an output-additive completeness gain **PROMOTED to default** in Iteration 12; existing gene lift unchanged). Four are kept **no-op aliases**: `--optimize` (best-of-outcome is now default), `--fast-align` (band-everything is now default — use `--full-dp-align` to opt out), `--parallel-aligners` (concurrent Step 4 is now default — use `--serial-aligners` to opt out), and `--lift-gene-like` (gene-like lift is now default — use `--gene-only` to opt out).

| Flag | Phase | What it changes |
|---|---|---|
| `--stream` | 7 | Pipe `miniprot` stdout into an in-memory gffbase FeatureDB; skip the `miniprot.gff3` disk round-trip + SQLite re-ingest. |
| `--inmemory-liftoff` | 8 | Serialise vendored Liftoff's `lifted_feature_list` to bytes in-process and feed gffbase directly; skip `liftoff.gff3` disk write + SQLite re-ingest. |
| `--threads N` + `--locus-pipeline` | 9 / Iter 8 | Dispatch Step 7 per-locus work through a `ThreadPoolExecutor`; emit in submission order via a heap-backed ordered writer (Phase 15d adds bounded spill-to-disk). Both flags required. **Iteration 8:** this now fans out on the **default (gffutils) backend WITHOUT `--native`** — *every* parallel run routes through the materialise + proxy-DB path (`process_locus_native`), so worker threads never touch the shared SQLite connection and any backend is thread-safe (pre-Iteration-8 a plain gffutils run silently downgraded to serial unless `--native`/`LIFTON_PARALLEL_FORCE`). `LIFTON_PARALLEL_BLOCK_GFFUTILS=1` opts back out to serial-on-gffutils; `LIFTON_PARALLEL_FORCE=1` still force-enables. Byte-neutral — the 24-cell matrix's `native=False, -t{2,4}` cells now genuinely exercise this path (no golden edit). A/B `benchmarks/compare/fresh_parallel_step7_ab.py` (`-t 8`, cached `-L`/`-M`, isolates Step 7): byte-identical across serial/parallel/parallel+native, Step-7 wall **−22 to −35%** (total **−19 to −29%**) + no-native↔native parity (drosophila 1.53×/−35% Step 7, mouse_to_rat chr18 1.29×, rice 1.35×). **Iteration 10:** the dispatch is now a single **FUSED** pool — each worker materialises its own locus (thread-local DB) then immediately processes it, instead of a 4-thread prefetcher pool building *all* payloads before a separate worker pool ran (a hard barrier). SQLite-bound materialise overlaps GIL-released parasail. Byte-neutral (`LIFTON_FUSE_STEP7=0` restores the two-phase path; `LIFTON_FUSE_MAT_CONCURRENCY=k` caps the materialise half). A/B `benchmarks/compare/fuse_step7_ab.py` (`-t 8`, cached `-L`/`-M`): byte-identical, completeness unchanged, Step-7 wall **−5 to −15%** (mouse_to_rat 1.18×, rice 1.12×, drosophila 1.05×) AND **peak RSS −26 to −44%** (fused never holds the full payload list — each locus is materialised→processed→freed). See memory [[lifton-step7-fresh-parallel]]. |
| `--native` | 10/11 | Drive `minimap2` through the `mappy` PyO3 binding in-process; route miniprot through the `pyminiprot`-shaped facade. **As of Phase 17b** the Step-7 threading unlock works on *any* backend via materialised proxy DBs; **as of Iteration 8 this no longer requires `--native`** — any `--threads N --locus-pipeline` run uses the proxy path (see the `--threads`/`--locus-pipeline` row), so `--native`'s remaining distinct value is the in-process miniprot facade. `LIFTON_PARALLEL_BLOCK_GFFUTILS=1` opts back out to serial-on-gffutils; `LIFTON_PARALLEL_FORCE=1` still force-enables threading. **Iteration 7:** `--native` no longer routes Liftoff's minimap2 alignment through `mappy` by **default** — that in-process path is **inferior to subprocess** (A/B `benchmarks/compare/native_liftoff_ab.py`: slower — per-query Python loop vs batched minimap2, bee +16% / drosophila +28% / mouse_to_rat **+124%** — and slightly less accurate cross-species, drosophila mean protein-id −0.016, since mappy's `Aligner` can't take `--end-bonus`/`-p`). So plain `--native` keeps Liftoff alignment on the proven **subprocess** path and delivers its value elsewhere (in-process miniprot facade + Step-7 threading unlock, ideally with a cached `-L`). The mappy Liftoff path is now **opt-in via `LIFTON_NATIVE_LIFTOFF_ALIGN=1`** (gated in `align_features.align_features_to_target`). It also had a **fresh-run crash bug** (independent of the fallback): the facade dropped `mm2_options`, losing `--eqx`, so mappy emitted `M` CIGAR the parser (counts only `=`/`X`) read as zero aligned bases → empty `liftoff.gff3` → exit 1; fixed in `native_bindings/minimap_facade._translate_mm2_options` (`extra_flags |= MM_F_EQX`, `best_n` from `-N`) so the opt-in path maps at gene-count parity. See memory [[lifton-native-liftoff-empty]]. |
| `--serial-aligners` (default is concurrent; `--parallel-aligners` = no-op alias) | Step 4 (Iter 6, **PROMOTED**) | Concurrent Step 4 is now the **default**: overlap the two independent external aligners — **miniprot** (subprocess, never touches `ref_db`) runs on a background worker thread while **Liftoff** runs on the **main thread** (Liftoff reads the main-thread-bound SQLite `ref_db`, so it cannot move off it; `sqlite3` forbids cross-thread use — same affinity issue Phase 17b solved for Step 7). Wall = max(t_liftoff, t_miniprot) instead of the sum; saving ≈ the smaller aligner's runtime. **`--serial-aligners`** restores the old sequential path. Byte-neutral promotion (only miniprot's *timing* moves; pinned by `tests/test_parallel_aligners.py` + the 24-cell matrix stays green since it now runs concurrent by default). A/B (`benchmarks/compare/parallel_aligners_ab.py`, `-t 8`, fresh Step 4, default vs `--serial-aligners`): Step-4 wall **−15–23%**, total wall **−5–11%** (diluted by serial Step 7 on subsets — larger when Step 7 is parallel) on bee/drosophila/mouse_to_rat, completeness preserved. Env-gated `LIFTON_PERF_STEP4=1` prints a Step-4 wall probe line to stderr. |
| `--strict-gff` | 5 | Run the NCBI GFF3 input-side validator on the reference annotation; exit non-zero on any spec violation. |
| `--validate-output` (+ `--validate-verbose`) | 13.5C | After writing, re-validate the output GFF3 with the in-tree `gff3_validator`; print a structured report. |
| `--legacy-merge` | 7 | **Changes output bytes.** Restore the pre-promotion *unconditional* Liftoff↔miniprot merge. The default now keeps, per transcript, the better of {merge+ORF, Liftoff+ORF} (best-of-outcome, `run_liftoff.process_liftoff_with_protein`); `--legacy-merge` reverts to applying the chained CDS unconditionally (manuscript reproduction / A-B baseline). `--optimize` is a kept no-op alias of the default. |
| `--full-dp-align` | Align (Iter 3) | **Changes output bytes.** Restore the pre-Iteration-3 *exact giant-only* alignment: full-DP `nw_trace_scan_sat` for every non-giant gene (gate 8000 aa / 25000 nt), giants still memory-bounded-windowed. The default now "bands everything" (anchor-windowed above 2500 aa / 8000 nt, cap 1500; `lifton/align.py:configure_alignment`). Env equivalents: `LIFTON_FULL_DP_ALIGN=1` (giant-only, for subprocess A/Bs); `LIFTON_ALIGN_WINDOW_{AA,NT}=<huge>` (pure full DP incl. giants — manuscript repro, OOM-prone). `--fast-align` is a kept no-op alias of the default. |
| `--gene-only` (default is gene-like lift; `--lift-gene-like` = no-op alias) | 2 (Iter 5; **PROMOTED Iter 12**) | **Changes default output (ADDS features).** The default now auto-detects every reference top-level parent type that has a transcript/exon hierarchy (`lifton_utils.get_gene_like_feature_types`) and lifts them all, not just `gene` — capturing pseudogenes / ncRNA_genes / structured mobile elements. It writes a temp `auto_feature_types.txt` and sets `args.features` so BOTH LiftOn and vendored Liftoff lift the expanded set; childless meta/regulatory types (region, enhancer, match) are excluded. **`--gene-only`** restores the pre-Iteration-12 gene-only lift (manuscript reproduction); **`--lift-gene-like`** is a kept no-op alias. Existing gene lift is unchanged — gene-like types are kept OUT of the miniprot-suppression IntervalTree so they can't displace a miniprot coding-gene rescue (`lifton_class.Lifton_GENE` adds only `gene`-type loci; tree seeded `["gene"]`), and the validator accepts exon/CDS under a pseudogene (`gff3_validator.DIRECT_EXON_PARENT_TYPES`). One-time bounded ref scan (`sample_cap=5000`/type) at startup on `-f`-less runs. A/B `benchmarks/compare/lift_gene_like_ab.py` (default vs `--gene-only`, Liftoff fresh): **+pseudogenes on all 5 — drosophila +21, mouse_to_rat +124, rice +158, arabidopsis +904, bee +1 — 0 genes lost, no new validity errors** (universal promotion gate). |

## Big-picture architecture

### The pipeline (`lifton/lifton.py:283-647`, `run_all_lifton_steps`)

Eleven steps + a strict-GFF gate, all in one strictly sequential function. Step numbers follow the in-source comments. (Line numbers drift with every phase — `grep -n "# Step"` to re-anchor.)

1. **Step 0 (~286):** Open target + reference FASTAs via `pyfaidx`, set up output dirs.
2. **Strict-GFF gate / input validator (~327-365):** Always runs `lifton.io.gff3_validator.GFF3Validator` on the reference annotation. Under `--strict-gff` it dumps findings per-row and **exits 2** on any spec error; otherwise (Phase 16 Tier 4) it writes findings to `lifton_output/stats/gff3_input_validation.txt` and emits a single summary line — this silenced the ~500K-line Dbxref stderr flood on RefSeq inputs.
3. **Step 1 (~368):** Build the reference `Annotation` (`lifton/annotation.py`) — gffutils SQLite by default, gffbase-DuckDB when `LIFTON_USE_GFFBASE=1`. GTF is auto-converted to GFF3 unless `--no-auto-convert-gtf`.
4. **Step 2 (~376):** Partition features into protein-coding vs non-coding via `lifton_utils.get_parent_features_to_lift` + `get_ref_liffover_features`. **As of Iteration 12** the default auto-detects every gene-like top-level parent type (`get_gene_like_feature_types`) and injects them into `args.features` (so both LiftOn and Liftoff lift pseudogenes / ncRNA_genes / … too); `--gene-only` opts back out to the `gene`-only lift.
5. **Step 3 (~383):** **Streaming extractor by default** (Phase 15b). `extract_sequence.extract_features_to_fasta` writes `transcripts.fa` + `proteins.fa` directly to `intermediate_files/`, then re-opens via `pyfaidx.Fasta` for lazy mmap-style access. The user's `-T` / `-P` branch takes the same `pyfaidx` path. The legacy `extract_features` (full in-memory dict) is still in the file for reference but not on the default path. **The historical "dominant memory hot-spot" caveat no longer applies on the default path.**
6. **Step 4 (~531):** Run vendored Liftoff in-process and miniprot as a subprocess. By default (Iteration 6) they run **concurrently** — miniprot on a background worker thread, Liftoff on the main thread (Liftoff reads the main-thread-bound SQLite `ref_db`, so it stays put); `--serial-aligners` restores the old sequential path (`--parallel-aligners` is a kept no-op alias). **As of Iteration 17** miniprot's own `-t` scales with LiftOn's `-t/--threads` (was hard-coded to miniprot's binary default of 4 regardless of `-t`, the Step-4 tail on cross-species data) — built by the shared `run_miniprot._build_miniprot_command`, gated on `threads > 1` so the default `-t 1` is byte-identical; `LIFTON_MINIPROT_THREADS=0` restores the old fixed default. Both can be **skipped** by passing pre-built GFFs via `-L` / `-M` (`lifton_utils.exec_liftoff` / `exec_miniprot` short-circuit on `os.path.exists`). Phase 16 wrapped the vendored-Liftoff call in `run_liftoff.py:57-83` with a `sys.setrecursionlimit(max(orig, 10000))` save/restore guard and a `traceback.format_exc()` dump on failure (the old code logged only `str(e)`, hiding which vendored frame recursed). **Note:** Liftoff's `-copies` multi-copy alignment is non-deterministic across fresh runs (tRNA repeat copies), so two independent fresh runs can differ by a few copy lines — independent of any LiftOn flag.
7. **Step 5 (~450):** Re-load the Liftoff and miniprot GFFs as gffutils DBs; open output writers. The "Creating … annotation database" log lines go through `_describe_annotation_source` (`lifton.py:9`) so an in-memory `bytes` blob (under `--inmemory-liftoff`/`--stream`) renders as `<in-memory bytes, N bytes>` instead of dumping a 30 MB GFF3 to stderr. Phase 15a emits the GFF3 directive prologue (`##gff-version 3` + every other `##` carried from the input) **before** any feature row, via `lifton.io.gff3_writer.format_directives`.
8. **Step 6 (~500):** Build the ref-id ↔ miniprot-id map (`lifton_utils.miniprot_id_mapping`) and seed per-chromosome `IntervalTree`s (`lifton/intervals.py`).
9. **Step 7 (~512):** Walk every Liftoff gene → build a `Lifton_GENE` → align with parasail → optionally chain with miniprot via `protein_maximization.chaining_algorithm` → ORF-rescue → write the entry. **Wall-clock hot-spot.** Dispatched through `lifton.parallel.parallel_step7` (`StepContext` is the immutable bundle; `LocusResult` is the submission-indexed return). With `--locus-pipeline` + `--threads N > 1`, work runs on a `ThreadPoolExecutor` (threads, not processes — gffutils/gffbase connections aren't picklable; parasail releases the GIL). **Phase 17b** materialises each locus on the parent thread into proxy DBs (`_RefDbProxy`/`_LFeatureDbProxy`/`_MFeatureDbProxy` in `locus_pipeline.py`) so worker threads never touch the SQLite connection — this is what made gffutils + `--native` thread-safe. **Phase 17c** added `_ThreadLocalCtxFactory`: when DBs are backed by on-disk files, the parent-thread materialise loop itself fans out across a 4-thread prefetcher pool (each thread re-opens `gffutils.FeatureDB(dbfn)`); falls back to a serial loop for in-memory/blob DBs. **Iteration 8** decoupled this from `--native`: *every* parallel Step-7 run (any backend) now routes through the materialise + proxy path, so `--threads N --locus-pipeline` fans out on a plain gffutils run with no `--native` (`_backend_supports_threads` returns True regardless of `native`; opt out via `LIFTON_PARALLEL_BLOCK_GFFUTILS=1`). **Iteration 10** FUSED the two phases: the 4-thread prefetcher pool + barrier + separate worker pool became ONE pool where each worker materialises its own locus (thread-local DB) then immediately processes it (on-disk path only; in-memory/blob keeps the serial-materialise fallback), overlapping the SQLite-bound materialise with GIL-released parasail and dropping peak RSS (no full payload list held in RAM). `LIFTON_FUSE_STEP7=0` restores the two-phase path. Output is emitted in submission order so `--threads N` is byte-identical to `--threads 1`.
10. **Step 8 (~548):** Walk miniprot-only mRNAs and emit any that pass the overlap/length filters.
11. **Step 9 (~566):** `stats.print_report`; close handles.
12. **Step 10 (~583):** When `--validate-output` is set, re-validate the just-written GFF3 with `lifton.gff3_validator` (hierarchy / CDS phase / containment / LiftOn-attr checks).

An orthogonal **evaluation mode** (`-E` / `-EL`, `lifton.py:413`) re-runs the per-locus loop through `run_evaluation.evaluation` to score an existing annotation instead of producing one; it writes `score.txt` and is the path the benchmark harness's eval pass drives.

### Vendored subtrees

Two subtrees of `lifton/` are external code in spirit and should be treated like frozen dependencies during refactors of the rest of `lifton/`:

- **`lifton/liftoff/`** — complete in-tree fork of upstream Liftoff (~3 KLOC, 18 files, no provenance marker). Invoked **as a library** (`run_liftoff.py:67` calls `liftoff_main.run_all_liftoff_steps(liftoff_args, ref_db)`, or `run_all_liftoff_steps_inmemory` at line 62 under `--inmemory-liftoff`). Liftoff itself shells out to `minimap2` (`liftoff/align_features.py`). Phase 11 added `lifton/liftoff/native_align.py`: when `args.native` is set, alignment is routed through `mappy` in-process via a `_PysamShim` that adapts mappy hits to the existing pysam-shaped consumer.
- **`lifton/gffbase/`** — first-party DuckDB-backed successor to gffutils (~3.6 KLOC + Rust extension `_native*.so`; MIT-licensed; same author). Imported under the `lifton.gffbase` namespace. Optional opt-in for the reference Annotation backend; fully drop-in for FeatureDB / Feature / DataIterator / GFFWriter. The `gffbase_adapter.py` shim translates between LiftOn's `Annotation` shape and gffbase's API. Phases 6.1–6.4 are the relevant plans.

### The `Lifton_TRANS` god module

`lifton/lifton_class.py` (896 LOC) is the central object model and the single most important file to understand. The class hierarchy:

```
Lifton_GENE  →  Lifton_TRANS  →  Lifton_EXON  →  Lifton_CDS
(57-217)        (267-820)        (826-873)       (876-895)
                god class —      thin gffutil    thin
                mixes 5          wrapper, ≤1     wrapper
                concerns         cds child
```

`Lifton_TRANS` (lines 267-820) currently mixes five orthogonal concerns:
- **Construction & exon/CDS bookkeeping** (~268-313)
- **Coordinate reconciliation** — the 5-case `update_cds_list` god method (315-)
- **Sequence assembly + alignment dispatch**
- **ORF rescue + CDS boundary patching** — `__find_orfs` (645-), `__update_cds_boundary` (718-)
- **GFF serialisation** — `write_entry` (794-)

The four-level write chain is `Lifton_GENE.write_entry → Lifton_TRANS.write_entry → Lifton_EXON.write_entry → Lifton_CDS.write_entry`, each delegating to gffutils' `__str__()` for the actual GFF3 line. The Phase 9 dispatcher splits parent-side write from worker-side processing: workers return a `LocusResult` and the parent thread is the only one that calls `write_entry`. This separation is what makes parallel output deterministic.

### Coupling shape

There is a star-shaped dependency around `lifton_utils` and `lifton_class`. **As of Iteration 16 the direct `lifton_utils`↔`lifton_class` package-level cycle is BROKEN** (it was previously "benign-but-fragile, kept working only by deferred attribute access"): the only thing `lifton_class` needed from `lifton_utils` was 3 pure helpers (`segments_overlap_length`, `custom_bisect_insert`, `get_ID_base`), now extracted into the dependency-free leaf module **`lifton/coreutils.py`**. `lifton_class` imports `coreutils` (not `lifton_utils`) and the redundant self-import was dropped; `lifton_utils` re-exports the 3 helpers from `coreutils` (so `lifton_utils.<helper>` still resolves) and keeps its one — now acyclic — top-level import of `lifton_class` (for `Lifton_TRANS`/`Lifton_feature`). The dependency is now a clean DAG `coreutils ← lifton_class ← lifton_utils` with **no lazy imports**. Byte-neutral (pure code-motion + reference-rename; 24-cell matrix green with no golden edit; 662 tests pass, 0 test edits — see *The byte-identity contract*). Newer modules (`parallel`, `locus_pipeline`, `io/gff3_writer`) sit cleanly outside the cycle and lazy-import where needed (e.g., `lifton.py:523` does `from lifton import parallel as _parallel`). Be cautious about reorganising imports — particularly anything that re-introduces an edge from `lifton_class` back to `lifton_utils` (route shared pure helpers through `coreutils` instead). Other top-level edges (`lifton_utils → run_liftoff`/`run_miniprot`) are out of scope of the Iteration-16 break and untouched.

## The byte-identity contract

The defining test of the whole codebase is the **24-cell matrix** at `tests/test_native_matrix.py::TestFullNativeMatrix::test_all_24_combinations_byte_identical`: every combination of `--stream × --inmemory-liftoff × --threads ∈ {1,2,4} × --native` (= 2·2·3·2 = 24 cells) must produce the same 391-byte output GFF3 as the default path. Smaller matrix gates exist for individual subsets:

- `tests/test_pipeline_streaming.py` — `--stream` byte-identity
- `tests/test_liftoff_inmemory.py` — `--inmemory-liftoff` 4-cell matrix
- `tests/test_parallelism_matrix.py` — `--threads` matrix
- `tests/test_native_matrix.py` — full 24-cell

Any change that touches the alignment kernel, the writer, the directive carrier, the ID-formation logic, or the per-locus result shape must keep this gate green. Phase 14's banded-alignment / mappy-seeded extension proposals were explicitly **deferred** because they would mutate alignment output by ~0.1% and break the gate.

**Scope clarification (best-of-outcome merge promotion, Iteration 1).** The 24-cell matrix pins *fast-path equivalence* (the `--stream × --inmemory-liftoff × --threads × --native` axes all reproduce the default), which is **orthogonal to the merge algorithm**. As of the best-of-outcome promotion, the default *merge* output is **no longer byte-frozen against the pre-promotion default**: when miniprot supplies a higher-identity chunk, the default now emits whichever of {merge+ORF-rescue, Liftoff+ORF-rescue} yields the higher emitted protein identity per transcript, instead of applying the chained CDS unconditionally. The 24-cell matrix stays green because the synthetic fixture has a perfect ORF (`liftoff_aln.identity == 1`) so the merge branch never fires there; `--legacy-merge` reproduces the pre-promotion bytes, and `tests/test_native_matrix.py::TestMergePromotion` (on the `merge_firing_workspace` fixture) pins the promoted behaviour: the merge fires (`status=LiftOn_chaining_algorithm`), `--optimize` is a byte-for-byte no-op alias, and the default never scores below `--legacy-merge`. The aggregate accuracy win is proven on real divergent data by `benchmarks/compare/legacy_merge_ab.py` (drosophila +0.0037 mean protein identity, 115 improved / 1 regressed; mouse_to_rat chr18 +0.0052, 91 improved / 0 regressed — both completeness-preserving, by reverting corrupting merges; the larger mouse chr2 run earlier confirmed +0.0067, 294/2). `mouse_to_rat` is pinned to chr18 (`NC_000084.7`, 2343 mRNA) for a ~3.5×-faster mammalian A/B; `benchmarks/compare/build_inputs.py` rebuilds one benchmark's `-L`/`-M` after a `ref_chrom` change. Promoting any further output-changing accuracy win follows the same recipe: keep the 24-cell matrix green (or deliberately re-baseline with a manuscript erratum), add a merge-firing/behaviour test, and prove the aggregate gain in the benchmark loop.

**Scope clarification (giant-gene windowed alignment, Iteration 2).** The two parasail base aligners (`lifton/align.py:parasail_align_protein_base`/`parasail_align_DNA_base`) call full-DP `nw_trace_scan_sat`, which is O(L²) in **time and memory** — a single titin alignment (~106 kb transcript) is tens of GB and OOM-crashes on mammalian genomes (the mouse benchmark peaked ~48 GB / 1264 s, almost all in ~14 giant genes; see memory [[lifton-giant-gene-align-blowup]]). Above a length gate the aligners route to `lifton/windowed_align.py`: unique-k-mer anchors → co-linear LIS chain → per-window full parasail → concatenated `.traceback` + a parasail-convention `.cigar` shim. The Iteration-2 gate was giant-only (8000 aa / 25000 nt); Iteration 3 (below) lowered it. `tests/test_windowed_align.py` pins losslessness, identity==full-DP, cigar compatibility, and gate routing. The giant-only gate (now reached via `--full-dp-align` / `LIFTON_FULL_DP_ALIGN=1`) changed output **only for above-gate giants**, in score attributes only (coding-giant `protein_identity` Δ = 0.0; long non-coding `dna_identity` ±0.001). Proven by `benchmarks/compare/align_window_ab.py` (mouse, giant-only-windowed vs forced full-DP, `-t 1`): **2.2× faster, 16.9× less peak RSS (44.9 GB → 2.6 GB), 0 non-giant transcripts differ.**

**Scope clarification (band-everything alignment is the default, Iteration 3).** Iteration 3 makes the anchor-windowed aligner the **default for all sizes**, not just giants: the gate is lowered to **2500 aa / 8000 nt** with a fine band (`WINDOW_CAP` 1500) and the exact-DP fallback raised to the giant boundary (`lifton/align.py:configure_alignment(band=True)`, applied at import). This **mutates normal-gene output** and is therefore *not* byte-frozen against the pre-Iteration-3 default — but the 24-cell matrix **stays green with no golden edit** because the synthetic fixtures (~33 aa / 600 nt) are far below the 2500 gate, so they still take exact full DP. Windowing is exact (== full DP) wherever anchors exist (the common homologous case) or a divergent region is below the giant boundary (the raised `max_fulldp` exact fallback); only true-giant anchor-less regions take the bounded approximate split. Escape hatches reproduce the older behaviour: `--full-dp-align` (exact giant-only path) and `LIFTON_ALIGN_WINDOW_{AA,NT}=<huge>` (pure full DP incl. giants, manuscript repro); `--fast-align` is a kept no-op alias. The aggregate win is proven on real data by `benchmarks/compare/fast_align_ab.py` (default vs `--full-dp-align`, `-t 1`): **mouse 2.59×, mouse_to_rat 2.14×, drosophila 1.43× wall; 3.5–4.5× less peak RSS**. Accuracy splits by divergence: **same-species mouse + mouse_to_rat = identity-exact (0 of 13,780 transcripts changed)**; cross-species drosophila is **mean-neutral** (16/7946 changed: 3 improved, 3 regressed by ≤1.2% on already-imperfect alignments; mean Δ ≈ 0) — completeness unchanged on all three. Promotion followed the loop's decision gate (speedup ≥5% wall AND mean identity Δ ≥ −1e-3 AND completeness unchanged). See memory [[lifton-giant-gene-align-blowup]] and [[lifton-optimization-flag-gating]].

**Scope clarification (concurrent Step 4 PROMOTED to default, Iteration 6).** Concurrent Step 4 (overlap Liftoff‖miniprot — miniprot → background thread, Liftoff stays on the main thread for SQLite `ref_db` affinity) is now the **default**, with **`--serial-aligners`** as the escape hatch and **`--parallel-aligners`** a kept no-op alias. This is a **byte-NEUTRAL promotion** (unlike the band-everything / best-of-outcome promotions, which changed bytes): only miniprot's *timing* moves, so the **24-cell matrix stays green with no golden edit** even though it now runs the concurrent path by default. Pinned by its own `tests/test_parallel_aligners.py` (default-concurrent vs `--serial-aligners`; default × stream × inmemory; default × native × `-t 4 --locus-pipeline`; `--parallel-aligners`==default no-op; plus thread-affinity guards — Liftoff on main, miniprot on a worker by default, both on main under `--serial-aligners`). A dedicated test, not a 5th 24-cell axis (orthogonal — Step-4 scheduling only). **Caveat for fresh-run A/Bs:** Liftoff's `-copies` multi-copy alignment is itself **non-deterministic across fresh runs** (two identical-config standalone Liftoff runs diverge in tRNA copy lines), so a fresh default-vs-`--serial-aligners` end-to-end diff on a repeat-rich genome (e.g. drosophila) is Liftoff noise, *not* the flag — which is why the byte-identity authority is the fixed-input unit test, not the fresh A/B. Speed win (`benchmarks/compare/parallel_aligners_ab.py`, `-t 8`, fresh, default vs `--serial-aligners`): Step-4 wall **−15–23%**, total **−5–11%** (saving ≈ the smaller aligner's runtime; larger when Step 7 is parallel). See memory [[lifton-optimization-flag-gating]] and [[lifton-step4-concurrency]].

**Scope clarification (fresh parallel Step 7 without `--native`, Iteration 8).** The Step-7 `ThreadPoolExecutor` unlock — previously gated behind `--native` (or `LIFTON_PARALLEL_FORCE`) so a plain gffutils run silently downgraded to serial — now fires on **any backend** for any `--threads N --locus-pipeline` run. The change is a one-line gate removal: `lifton/parallel.py:_backend_supports_threads` returns True regardless of `native`, and the `payloads` materialisation (the byte-safe `MaterialisedLocus` → proxy-DB → `process_locus_native` path, already backend-agnostic in `locus_pipeline.py`) is built whenever pooling, not just `if native_active`. This is another **byte-NEUTRAL** change (like the Iteration-6 promotion): the **24-cell matrix stays green with no golden edit** — and, crucially, its `native=False, -t{2,4}` cells (which pass `--locus-pipeline`) flip from a silent serial downgrade to *genuine non-native parallel* runs executing the **same proxy code** as the already-green `native=True` cells, so the matrix now actively pins the new path. `LIFTON_PARALLEL_BLOCK_GFFUTILS=1` restores the pre-Iteration-8 serial-on-gffutils fallback. Pinned by `tests/test_fresh_parallel_step7.py` (default-backend `-t4 --locus-pipeline` no-`--native` == serial; a real pool IS created; no-native == native) and the updated `tests/test_parallelism_matrix.py::TestLocusPipelineSemantics`. Speed: `benchmarks/compare/fresh_parallel_step7_ab.py` (`-t 8`, cached `-L`/`-M` to isolate Step 7 + dodge the Liftoff `-copies` fresh-run noise) — byte-identical across {serial, parallel-no-native, parallel-native} on all three, no-native↔native parity (≤1.5 s), completeness unchanged; Step-7 wall **−22 to −35%** / total **−19 to −29%** (drosophila 1.53×/1.40×, mouse_to_rat chr18 1.29×/1.23×, rice 1.35×/1.24×). Bee-scale is muted (Step 7 a small fraction). See memory [[lifton-step7-fresh-parallel]] and [[lifton-optimization-flag-gating]].

**Scope clarification (fused Step-7 pool, Iteration 10 — byte-NEUTRAL, SHIPPED).** The parallel Step-7 dispatch (`lifton/parallel.py:parallel_step7`) ran as **two sequential phases with a hard barrier**: a 4-thread prefetcher pool built *all* `MaterialisedLocus` payloads (SQLite-bound), drained fully, *then* a separate N-thread worker pool ran *all* the parasail processing — wall = `T_materialise(4) + T_process(N)`, never overlapping (this is why Iteration 8 capped at 1.53× at `-t8`). Iteration 10 **fuses** them: when `_ThreadLocalCtxFactory.viable` (on-disk DBs), one pool runs `_mat_and_process` per locus — each worker materialises its own locus via the thread-local factory, then immediately calls `process_locus_native` — so the SQLite-bound materialise overlaps the GIL-released parasail and wall collapses toward `max(T_mat, T_proc)`. In-memory/blob backends keep the serial-parent-materialise + worker-pool fallback. This is the **third BYTE-NEUTRAL change** (after Iter-6 concurrent Step 4 and Iter-8 fresh parallel Step 7): same `materialise_locus_with_factory` payloads + same `process_locus_native` → same `LocusResult`, only the *scheduling* changes, so the **24-cell matrix stays green with no golden edit** (its on-disk gffutils cells now take the fused path and pin it). Escape hatch `LIFTON_FUSE_STEP7=0` restores the two-phase path; `LIFTON_FUSE_MAT_CONCURRENCY=k` caps how many workers may be in the materialise half at once (the lever if SQLite contention ever regresses — unused, no regression seen). Pinned by `tests/test_fuse_step7.py` (fused builds exactly ONE pool + byte-identical to serial; `LIFTON_FUSE_STEP7=0` → TWO pools + byte-identical; a materialise-half failure becomes a per-locus error `LocusResult`, siblings still emit). A/B `benchmarks/compare/fuse_step7_ab.py` (`-t 8`, cached `-L`/`-M`, `LIFTON_FUSE_STEP7=0` two_phase vs default fused): **byte-identical on all three, completeness unchanged, Step-7 wall −5 to −15%** (mouse_to_rat chr18 1.18×/−15.3%, rice 1.12×/−10.8%, drosophila 1.05×/−5.0%; modest because the two-phase baseline already prefetched materialise across 4 threads) **AND peak RSS −26 to −44%** (the fused path never holds the full payload list — each locus is materialised→processed→freed; the bigger win at mammalian scale). rice (the most materialise-heavy, the contention canary) showed no regression. See memory [[lifton-step7-fresh-parallel]] and [[lifton-optimization-flag-gating]].

**Scope clarification (concurrent Step-5 DB build — Iteration 11 NO-GO, code REVERTED).** Iteration 11 tried to build the two independent Step-5 gffutils DBs (liftoff, miniprot) **concurrently** — the miniprot DB on a worker thread (then reopened on the main thread by its on-disk `.dbfn` to sidestep SQLite thread-affinity), liftoff on the main thread — gated by `LIFTON_CONCURRENT_DBBUILD` + a `LIFTON_PERF_STEP5` probe. It was **byte-identical** on all three datasets (the worker writes the deterministic `.db` file; the main reopens it → identical queries) AND completeness-preserving — **but consistently SLOWER at the Step-5 build itself** (A/B `benchmarks/compare/dbbuild_concurrent_ab.py`, cached `-L`/`-M`: mouse 28.4→30.8s **−8.4%**, drosophila 11.9→13.2s −11.6%, rice 14.0→15.1s −8.3%; total-wall deltas were Step-6/7/9 noise). **Root cause: the GIL.** `gffutils.create_db` is **GIL-bound Python** (line-by-line GFF parse + Feature-object construction + bin computation; only the brief SQLite `executemany` releases the GIL), so two builds on two *threads* time-slice on one core with GIL contention + thread/reopen overhead instead of running in parallel. This is the inverse of why the prior scheduling wins worked: **concurrent Step 4** overlaps a *subprocess* (true parallelism) and **Step-7 fusion** overlaps *parasail* (GIL-released) — a pure-Python gffutils build threads-parallelizes to nothing. **Decision: NO-GO** (a default-on change that is *slower* must not ship). True parallelism would need a separate *process* (own GIL), but the ceiling is only ~2.5% total wall (mammalian-only, ≈ the smaller miniprot build) and it forks mid-pipeline (open gffutils/pyfaidx handles) — not worth the complexity/risk. The `lifton/` change was **REVERTED**; the **audit trail is the retained `benchmarks/compare/dbbuild_concurrent_ab.{py,json,md}`** + this note. Lesson: thread-based concurrency only helps GIL-RELEASING work (subprocesses, parasail) — never a CPU-bound pure-Python gffutils build. See memory [[lifton-step4-concurrency]] and [[lifton-optimization-flag-gating]].

**Scope clarification (gene-like lift PROMOTED to default, Iteration 12 — output-additive).** The opt-in `--lift-gene-like` (Iteration 5) is now the **default**: the `-f`-less default run auto-detects every reference top-level parent type with a transcript/exon hierarchy (`lifton_utils.get_gene_like_feature_types`) and lifts them all (pseudogenes / ncRNA_genes / structured mobile elements), not just `gene`. The injection condition flipped (`lifton.py`: `if not args.gene_only and args.features is None`); **`--gene-only`** is the opt-out (restores the pre-Iteration-12 gene-only lift), and **`--lift-gene-like`** is a kept no-op alias (same pattern as `--optimize`/`--fast-align`/`--parallel-aligners`). This **CHANGES default output (ADDS features)** — so, like the best-of-outcome merge / band-everything promotions and unlike the byte-neutral scheduling flips, it is *not* byte-frozen against the pre-Iteration-12 default; `--gene-only` reproduces the old bytes (manuscript erratum). The **24-cell matrix stays green with NO golden edit** because both its fixtures (`integration_workspace`, `merge_firing_workspace`) are **gene-only** → auto-detect returns `["gene"]` → the injected `auto_feature_types.txt` is a no-op (≡ `args.features=None`) → byte-identical there; real annotations with pseudogenes/ncRNA_genes now gain those features. Two safety guards (unchanged): gene-like loci are kept OUT of the miniprot-suppression IntervalTree (`tree_features=["gene"]` when injected; `Lifton_GENE` adds only `gene`-type) so a pseudogene can't displace a miniprot coding-gene rescue (the CG3303/Plac8l1 fix), and the validator accepts exon/CDS under a pseudogene (`gff3_validator.DIRECT_EXON_PARENT_TYPES`). Pinned by `tests/test_integration_pipeline.py::TestLiftGeneLike` (default emits the pseudogene, `--gene-only` omits it, `--lift-gene-like` is a byte-identical no-op alias). The completeness win is proven **universally** by `benchmarks/compare/lift_gene_like_ab.py` (default vs `--gene-only`, Liftoff fresh, `off⊆on` gate): **all 5 datasets PROMOTE — drosophila +21, mouse_to_rat +124, rice +158, arabidopsis +904, bee +1 pseudogenes, 0 genes lost, no new validity errors.** See memory [[lifton-fullfeature-lift-gap]] and [[lifton-optimization-flag-gating]].

**Scope clarification (Step-8 miniprot-rescue tuning — Iteration 13 NO-GO, code REVERTED).** The remaining completeness lever after the gene-like promotion was Step-8 (`run_miniprot.process_miniprot`): the overlap (`-overlap` 0.1) + length-ratio (`-min_miniprot`/`-max_miniprot` 0.9/1.5) filters drop miniprot-only mRNA candidates that *might* be genuinely-missing transcripts. **Feasibility-first** (Iter-4/9 recipe): a byte-neutral `LIFTON_STEP8_DIAG` probe (`check_ovps_ratio(return_max=True)` + a gated drop-reason TSV) ran first to quantify the *near-miss* headroom (drops just past the threshold — the upper bound on what relaxing could recover). `benchmarks/compare/step8_rescue_headroom.py`: the filters are **well-tuned** — ~99.9% of overlap drops sit in (0.5,1.0] (the miniprot mRNA *is* the gene Liftoff already lifted) and length drops are mostly clearly-truncated (<0.8); absolute near-miss was **5 candidates on drosophila, 0 on mouse_to_rat** (the latter has 0 miniprot-only rescues at all). The A/B (`step8_rescue_ab.py`, default vs relaxed `-overlap 0.2 -min_miniprot 0.8 -max_miniprot 1.6`, via the existing CLI args — no code change) **adversarially confirmed on emitted output**: relaxing adds only **2 transcripts on drosophila at mean protein identity 0.35** (both <0.7 — low-identity garbage; overall mean PI even ticks down) and **0 on mouse_to_rat**. **Decision: NO-GO** — the Step-8 defaults are well-tuned; relaxing admits only false rescues. The only `lifton/` code was the byte-neutral instrumentation, **REVERTED** (Iter-9/11 precedent); the audit trail is the retained `benchmarks/compare/step8_rescue_{headroom,ab}.{py,json,md}` + this note (the A/B remains runnable via CLI threshold args; the headroom diagnostic needs the reverted `LIFTON_STEP8_DIAG`). Both the accuracy *and* this completeness lever are now mapped dry. See memory [[lifton-fullfeature-lift-gap]] and [[lifton-optimization-flag-gating]].

**Scope clarification (miniprot-only fallback — Iteration 15 NO-GO, code REVERTED).** The one genuinely-unexplored *structural* lever (vs the dry *tuning* levers) was that LiftOn never falls back to a **miniprot-only** model for a coding gene — confirmed in code as two gaps sharing that root cause. **Gap #1:** the best-of-outcome merge (`run_liftoff.process_liftoff_with_protein`) only ever picks `{chained-merge+ORF, Liftoff+ORF}` — never a standalone miniprot-only candidate. **Gap #2:** Step 8 (`run_miniprot.process_miniprot`) drops a miniprot-only mRNA for *any* overlap with a Liftoff locus, regardless of that lift's quality (distinct from Iter-13, which only relaxed the *thresholds*; this was a Liftoff-**quality**-aware gate). **Feasibility-first** (Iter-4/9/13 recipe): two byte-neutral diagnostics ran first (`LIFTON_MINIPROT_ONLY_DIAG` in run_liftoff; `LIFTON_MINIPROT_RESCUE_DIAG` in run_miniprot — the latter scores suppressed candidates on a *throwaway tree* so the real suppression tree is untouched). **Gap #1 headroom** (`benchmarks/compare/miniprot_only_headroom.py`): a miniprot-only merge candidate would change only 26/6699 (drosophila) / 20/1983 (mouse_to_rat) merge-fired transcripts — projected corpus mean-PI Δ **+0.00005 / +0.00041**, far below +0.003 → **NO-GO at headroom** (the 2-way merge already captures miniprot at the chaining sync points; reverted, never reached an A/B). **Gap #2 headroom** looked promising (drosophila **44** "recoverable" at mean mp_aa 0.925 / weak local Liftoff PI 0.68; mouse_to_rat 8), so the `--miniprot-rescue-weak-liftoff` feature (+ env `LIFTON_MINIPROT_RESCUE_WEAK`; opt-in, byte-identical default; an index of Step-7 Liftoff PIs built post-Step-7 by reading the flushed score.txt — no Step-7 plumbing, no concurrency) was BUILT and A/B'd. The A/B (`miniprot_only_rescue_ab.py`) **adversarially revealed what the upside-only headroom could not**: the recoverable population is **overwhelmingly DUPLICATES** of weakly-lifted ref genes — drosophila **n_added=5** genuinely-new genes (mean PI 0.905, 0 regressed, 0 lost, no new validity errors) but **44 extra gene models** (the "36 common improved" is an artifact of the max-per-ref evaluator crediting an additive *redundant overlapping* model — the weak Liftoff gene stays in the output); mouse_to_rat **n_added=0** (all 8 rescues duplicate already-lifted genes). mean-PI Δ +0.0009 / +0.0025, still below the bar. Replacing weak genes in place is exactly Gap #1 (already NO-GO). **Decision: NO-GO** — genuine missing-gene recovery is negligible (5/0) and the dominant effect is redundant duplicate models. Both gaps' `lifton/` code (diagnostics + the Gap-#2 feature/flag) was **REVERTED**; the audit trail is the retained `benchmarks/compare/miniprot_only_{headroom,rescue_headroom,rescue_ab}.{py,json,md}` + this note (the headroom drivers need the reverted instrumentation to re-run; the A/B needs the reverted flag). Jointly with Iter-13 this triangulates that Step-8 suppression correctly drops redundant duplicates and the miniprot-only signal the pipeline doesn't already capture is negligible. Lesson reinforced (the Iter-9 lesson, again): the headroom gates cheaply but only measures upside; the A/B catches that "recoverable" candidates were duplicate models, not new genes. See memory [[lifton-fullfeature-lift-gap]], [[lifton-chaining-at-safe-ceiling]], and [[lifton-optimization-flag-gating]].

**Scope clarification (import-cycle break — Iteration 16, byte-NEUTRAL, SHIPPED).** First *engineering* iteration after the accuracy (Iter 4/9/15) and completeness (Iter 13/15) wells were mapped dry. The fragile `lifton_utils`↔`lifton_class` package-level cycle (CLAUDE.md called it "benign-but-fragile, kept working only by deferred attribute access") was **broken into a clean DAG**. The only thing `lifton_class` pulled from `lifton_utils` was 3 *pure* helpers (`segments_overlap_length`, `custom_bisect_insert`, `get_ID_base`); they were moved verbatim into a new dependency-free leaf module **`lifton/coreutils.py`**, `lifton_class` now imports `coreutils` (and the redundant self-import `lifton_class` was dropped), and `lifton_utils` re-exports the 3 from `coreutils` so every `lifton_utils.<helper>` reference (incl. tests) still resolves — DAG `coreutils ← lifton_class ← lifton_utils`, **no lazy imports**. This is a **pure code-motion + reference-rename** (no logic touched) — the **fourth byte-NEUTRAL change** (after Iter-6 concurrent Step 4, Iter-8 fresh parallel Step 7, Iter-10 fused Step-7 pool): the **24-cell matrix stays green with NO golden edit** and the **full 662-test suite passes with 0 test edits** (leaving `Lifton_ORF` defined in `lifton_class` + the `lifton_utils` re-export kept all existing references valid). Detail in *Coupling shape* above. The larger `Lifton_TRANS` god-module concern-split (data classes → `write_entry` serializer → ORF-rescue → `update_cds_list`) that this unblocks was deliberately **deferred** (the refactor track was paused here). See memory [[lifton-import-cycle-break]].

**Scope clarification (miniprot thread plumbing PROMOTED to default — Iteration 17, byte-NEUTRAL, SHIPPED).** miniprot was launched with **NO `-t` flag** (`run_miniprot.py` built `[miniprot, "--gff-only", tgt, ref_proteins] + mp_options.split()`; `mp_options` defaults to `''`), so it always ran at its hard-coded binary default of **4 threads — regardless of LiftOn's `-t/--threads N`** (minimap2, by contrast, already scaled: `liftoff/align_features.py:44` gives each minimap2 `floor(args.threads / n_chroms)`). On cross-species data miniprot does heavy protein alignment and, capped at 4 while minimap2 scaled, became the serial tail of the concurrent Step 4 whenever it was the larger aligner. Iteration 17 plumbs `args.threads` into miniprot's own `-t` via a new shared helper `run_miniprot._build_miniprot_command` (used by BOTH the subprocess path and the `--native --stream` facade `MiniprotIndex.align_all`, which now takes a `threads` kwarg). **This is the 5th BYTE-NEUTRAL change** (after Iter-6 concurrent Step 4, Iter-8 fresh parallel Step 7, Iter-10 fused Step-7 pool, Iter-16 import-cycle break). Three design decisions make it byte-safe and non-regressing: (1) **gated on `threads > 1`** — at the default `-t 1` NO `-t` is emitted, so the command is byte-identical to pre-Iteration-17 and miniprot keeps its own default 4 (emitting `-t 1` would be a 3.7× *slowdown* — measured: standalone miniprot 41.8s at `-t1` vs 11.2s at its `-t4` default); (2) an **exact/glued-token check** (`opt.startswith("-t")`, NOT a bare substring) so an explicit user `-t`/`-t8` in `mp_options` is respected without duplication and `--trans` doesn't false-positive and suppress the flag; (3) call sites read `getattr(args, "threads", 1)` so an `args` without `.threads` can't crash. Escape hatch / A-B baseline switch **`LIFTON_MINIPROT_THREADS`**: unset → `args.threads` (gated >1); `=0` → never add `-t` (reproduce the old fixed default); `=k≥1` → force `-t k` (bypasses the gate). **The 24-cell matrix stays green with NO golden edit** — and is structurally unaffected: it uses cached `-L`/`-M`, so `exec_miniprot` short-circuits and real miniprot is **never invoked** in any test; byte-neutrality is therefore a real-data property, proven by (a) a standalone-miniprot **feasibility gate** — `miniprot --gff-only` output is **byte-identical across `-t 1/4/8/16`** on drosophila + mouse_to_rat (Heng Li `kt_pipeline` preserves input order, so output order is thread-stable; this also moots the gffutils-insertion-order question since `features_of_type('mRNA')` at Step 8 yields rowid = file order) and (b) 19 new unit tests pinning the command seam (`tests/test_miniprot_threads.py`; full suite 662→**681**, 0 edits to existing tests). A/B `benchmarks/compare/miniprot_threads_ab.py` (`-t 8`; ARM 1 = cached `-L` + fresh miniprot so Step-4 wall == miniprot wall, ARM 2 = fresh both / concurrent Step 4): **byte-identical 6/6 across both arms, completeness unchanged 3/3, miniprot/Step-4 wall −39 to −42% (1.65–1.72×)** on drosophila/mouse_to_rat/rice, **no oversubscription harm** (worst ARM-2 Δ −0.5% = noise; 48-core box, `-t 8`). The win lands in *total* wall whenever miniprot is the Step-4 tail (cached-`-L` reruns, `--serial-aligners`, divergent species, large genomes — drosophila ARM-2 total **−12.4%**); under concurrent Step 4 where **minimap2** is the tail (mouse_to_rat/rice subsets) miniprot's speedup is absorbed under the overlap (Step-4 wall ≈ flat) but never regresses. `--serial-aligners` remains the lever if a core-constrained box ever oversubscribes. See memory [[lifton-miniprot-thread-plumbing]] and [[lifton-optimization-flag-gating]].

**Scope clarification (ORF-rescue best-match selection — Iteration 9 NO-GO, code REVERTED).** The accuracy axis this round targeted ORF *selection*: `lifton_class.__find_orfs` keeps only the single LONGEST ORF per reading frame (`i = orf_idx_e` interior-skip), then identity-scores those ≤3 survivors — so a shorter but better-matching ORF is discarded. The experiment (gated behind `--orf-best-match` / `LIFTON_ORF_BEST_MATCH=1`) kept the top-K(=3) longest per frame (no-interior-skip scan, trailing-`*` stripped) and selected by reference-protein identity. **Feasibility-first** (the Iteration-4 recipe): a byte-neutral `LIFTON_ORF_DIAG` probe ran FIRST (flag OFF, output byte-frozen) measuring, per rescued transcript, what the broadened pick *would* achieve vs the applied longest — `benchmarks/compare/orf_best_match_headroom.py`: drosophila **5/6979** rescued transcripts would change (projected corpus mean-PI Δ **+0.00011**), mouse_to_rat chr18 **12/2305** (**+0.00030**), both ~10-30× below the +0.003 promotion bar (and the +0.005 Iteration-4 bar). The A/B (`benchmarks/compare/orf_best_match_ab.py`, default vs `LIFTON_ORF_BEST_MATCH=1`, cached `-L`/`-M`) then **adversarially verified on emitted output** what the flag-OFF projection could not see — the strip/re-score **ripples into the best-of-outcome merge**: net **−0.00003** (drosophila, 4 improved / **59 regressed**) and **+0.00008** (mouse_to_rat, 3 / **23**), with CDS counts perturbed (regressions dominate; gate 0/2). **Decision: NO-GO.** The longest ORF per frame is already near-optimal — the same "well is dry" shape as Iteration 4 (when one source/choice wins a transcript it usually wins everywhere). Because the result was **net-negative** (not merely marginal), the `lifton/` implementation was **REVERTED** (no flag shipped, matching the Iteration-4 precedent); the **audit trail is the retained benchmark scripts + results** (`benchmarks/compare/orf_best_match_{headroom,ab}.{py,json,md}`) plus this note. To reproduce, restore the `LIFTON_ORF_BEST_MATCH` / `LIFTON_ORF_DIAG` instrumentation in `lifton_class.__find_orfs`. Lesson reinforced: the feasibility diagnostic gates cheaply, but the A/B is what catches downstream ripple regressions an upside-only projection misses. See memory [[lifton-chaining-at-safe-ceiling]] and [[lifton-optimization-flag-gating]].

## Test-suite guarantees

- The integration tests in `tests/test_integration_pipeline.py` use the `hermetic_pipeline` fixture, which monkey-patches `run_liftoff.run_liftoff` and `run_miniprot.run_miniprot` to **raise** if invoked. CI machines do not need `minimap2` or `miniprot` installed for pytest to pass, and any future regression that accidentally falls through to the external runners trips a hard failure.
- Golden-path testing strategy: provide `-L` and `-M` with pre-baked GFFs so `exec_liftoff` / `exec_miniprot` short-circuit on `os.path.exists`. The same trick is the right way to add new integration scenarios.
- `tests/conftest.py` builds 600-bp synthetic chromosomes whose positions 101-199 (`ATG…`) and 301-399 (`…TAA`) form a clean ORF. Reuse those fixtures rather than authoring new FASTA from scratch.
- Opt-in performance / memory profiling lives under `tests/perf/` and is gated by the `perf` pytest marker (registered in the root `conftest.py`); nothing under `tests/perf/` runs by default.
- A small handful of property-based tests (`test_property_based.py`, `test_streaming_property.py`, `test_vulnerabilities.py`) `import hypothesis` at module scope, so they **ERROR on collection** (not skip) when `hypothesis` is absent — `pytest tests/` reports "524 collected, 3 errors". Install `hypothesis` or deselect those three files for a clean run. The rest of the suite has no extra dependency beyond what conda installs.
- `tests/test_native_bindings.py` deliberately asserts `mappy` is installed (the test env ships it as of Phase 16); without `mappy` those cases fail rather than skip.

### Biological validation benchmarks (`benchmarks/`)

Phase 16 added a real-data harness (`benchmarks/run_benchmarks.py`, registry `datasets.json`) that downloads, lifts, and evaluates the five published JHU CCB same-species datasets (human, mouse, bee, arabidopsis, rice). It shells out to a real `lifton` install (not the hermetic test fixtures), parses GNU/BSD `/usr/bin/time -v` into a `summary_*.json`, and is **not** part of `pytest` — its own unit tests live in `tests/test_benchmark_harness.py`. Phase-17 wall-clock runs use `benchmarks/phase17_rerun.sh` (parameterised by `$DATASET`) and `benchmarks/phase17_human_subset.sh`. Cached inputs/outputs live under `benchmarks/data/` and `benchmarks/results/`. Known open issue: the optional `-E` evaluation pass exits 1 with `mapped: 0` (a harness-side score-parsing mismatch, not a lift-step bug).

## Refactor history & what's in flight

The staged refactor that the existing audit anticipated has largely landed. Plans in `plans/` document each phase end-to-end:

- `plans/phase_1_audit.md` — read-only architectural audit (historical baseline)
- `plans/phase_2_bottlenecks.md` — the original prioritised-targets list (memory, god-module split, parallelism, packaging)
- `plans/phase_3_test_plan.md` — the test-suite shape and the original three pinned bugs
- `plans/phase_4_5_algorithmic_trust.md`, `plans/phase_5_bug_elimination.md` — **landed** (commit `3e2db85`): all three of the original "pinned legacy bugs" are now fixed (`Lifton_GENE` ID built as a list `[gene_id]`, transcript Parent inherits the corrected gene id, `check_ovps_ratio` no longer passes a raw tuple). The `xfail` markers were removed.
- `plans/phase_6_*.md` — vendored gffbase landing (audit → migration → integration)
- `plans/phase_7_streaming_execution.md` — `--stream` (Phase 7)
- `plans/phase_8_liftoff_inmemory_execution.md` — `--inmemory-liftoff` (Phase 8)
- `plans/phase_9_locus_fusion_execution.md` — `parallel.py` + `locus_pipeline.py` + `--threads` / `--locus-pipeline` (Phase 9)
- `plans/phase_10_native_bindings_execution.md` — `lifton/native_bindings/` mappy + pyminiprot facade (Phase 10)
- `plans/phase_11_inloop_rewiring_execution.md` — `--native` in-loop rewiring + thread-safety unlock (Phase 11)
- `plans/phase_12_manuscript_audit.md` — manuscript-vs-code parameter audit
- `plans/phase_13_edge_cases.md`, `plans/phase_13_5{A,B,C}_*.md` — algorithmic hardening + GFF3 formatting fixes (Phase 13)
- `plans/phase_14_optimization_proposal.md` — proposal-only HPC redesign; banded alignment / mappy-seeded extension excluded as byte-output-mutating
- `plans/phase_15_optimization_execution.md` — byte-safe Phase 14 subset shipped: directive carrier (15a), streaming RefSeqProvider (15b), bounded miniprot drain (15c), bounded ordered-writer with on-disk spill (15d)
- *Latest commit (`7152cca`):* `phase-16` — biological validation benchmark harness under `benchmarks/`.
- `plans/phase_16_diagnostic_and_fixes.md` — **uncommitted in the working tree.** Fixes that made the bee benchmark exit 0: GNU-time parser (Tier 1), traceback + 10K recursion-limit guard in `run_liftoff.py` (Tier 2/3), gated the GFF3-validator stderr flood (Tier 4), declared `mappy` in `setup.py`/`lifton.yml` (Tier 5), and the `_describe_annotation_source` bytes-blob log helper.
- `plans/phase_17b_materialisation_refactor.md` — **uncommitted.** The materialisation refactor: per-locus workers read from proxy DBs built from a pre-fetched payload, so `lifton/parallel.py:_backend_supports_threads` now returns True for any backend under `--native` (opt out with `LIFTON_PARALLEL_BLOCK_GFFUTILS=1`). Byte-identical by construction; bee wall-clock was flat (Step 4 subprocess, not Step 7, dominates at bee scale).
- `plans/phase_17c_followon.md` — **uncommitted.** Verified the unlock pays off at scale (rice: 2.6× per-feature speedup vs bee's flat 1.0×) and shrank the parent-thread materialise overhead (Variant-3 query dedup + the `_ThreadLocalCtxFactory` 4-thread prefetcher pool). Items 3 (Step-4 attack) and 4 (DAG-fusion algorithmic rewrite) are explicitly deferred — both would mutate byte output.

**Working-tree state:** the CLAUDE.md you're reading and the Phase 16/17 changes above are **uncommitted** (`git status` shows modified `lifton/{lifton,parallel,locus_pipeline,run_liftoff,annotation}.py`, `setup.py`, `lifton.yml`, benchmark/test additions). 524 tests collect and pass under the `lifton_devel` env.

Read the phase plans for any structural change. The core invariant for any future work is the **24-cell byte-identity matrix**: refactors below that line are free; refactors above it require a deliberate test edit and a manuscript erratum.
